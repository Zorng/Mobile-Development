void main() {
  Map<Map<int, int>, String> something = {
    {1:1}: "Carr",
    {2:1}: "dog"
  };

  Map<Map<int, int>, String> item = {
    {1:10}: "Dino",
  };


  print(item.keys.first.keys);

  for (var entry in something.entries) {
    if(item.k)
  }

  something.keys.first == 1? print(true) : print(false);
}